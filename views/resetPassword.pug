extends loginLayout
block contenido
    h1 Blanquear contraseña

    if mensajes
        .alertas
            each categoria in Object.keys(mensajes)
                each mensaje in mensajes[categoria]
                    .alerta(class=`${categoria}`)
                        p= mensaje
    
    form#formulario.caja.caja-login(method="POST")
        .campo
            label(for="password") Contraseña: 
            input(type="password" name="password" placeholder="Ingrese nueva contraseña...")
        .campo.enviar
            input.boton(type="submit" value="Resetear")
        .campo.acciones
            a(href="/iniciar-sesion") Volver a inicio de sesión de cuenta